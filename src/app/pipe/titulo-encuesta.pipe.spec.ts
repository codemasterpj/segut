import { TituloEncuestaPipe } from './titulo-encuesta.pipe';

describe('TituloEncuestaPipe', () => {
  it('create an instance', () => {
    const pipe = new TituloEncuestaPipe();
    expect(pipe).toBeTruthy();
  });
});
